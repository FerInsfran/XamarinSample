<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinSample.InteractionPage">
    <ContentPage.Content>
        
        <StackLayout>


            <Label Text="Enter the two desired values" 
                   FontSize="30"
                   TextColor="Red"
                   FontAttributes="Bold"
                   HorizontalOptions="CenterAndExpand"/>

            <Entry x:Name="firstValueEntry" Placeholder="First Number" Keyboard="Numeric"/>
            
            <Entry x:Name="secondtValueEntry" Placeholder="Second Number" Keyboard="Numeric"/>

            <Button x:Name="interactionButton"
                    Text="Add the Values!"
                    Clicked="interactionButton_Clicked"/>
           
            <Label x:Name="resultLabel" 
                   Text=""
                   FontSize="50"
                   TextColor="Green"
                   FontAttributes="Bold"
                   HorizontalOptions="CenterAndExpand"/>





        </StackLayout>
    </ContentPage.Content>
</ContentPage>